#!/bin/bash
# Add orgmode paths here
ORGMODE_PATHS=""

if ! path=$(echo "$ORGMODE_PATHS" | fzf)
then
    echo "No path was specified"
    exit 1
fi

if ! cd "$path"
then
    echo "Couldn't change to $path"
    exit 1
fi
nvim -u ~/.config/nvim/init_org.lua -c "lua require('fzf-lua').files()"
