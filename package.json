{
    "scripts": {
        "lint": "prettier --check --plugin-search-dir=. .",
        "format": "prettier --write --plugin-search-dir=. --cache --cache-strategy metadata ."
    },
    "devDependencies": {
        "prettier": "^2.7.1"
    }
}
